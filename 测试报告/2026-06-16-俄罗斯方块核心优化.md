# 俄罗斯方块核心优化测试报告

测试日期：2026-06-16
测试人：Codex

## 变更范围

- 俄罗斯方块从单文件实现拆分为 Page / Types / Rules / Theme / Board / Hud / Preview / Control / Overlay。
- 棋盘从 ArkUI `Grid + 200 GridItem` 改为 Canvas 绘制。
- 游戏推进改为状态机 + 按需下落 / 锁定计时。
- 规则层新增 node mjs 回归测试。

## 自动化测试

命令：

```bash
node --no-warnings scripts/test-tetris-rules.mjs
```

结果：

- 28 passed, 0 failed

覆盖点：

- 空棋盘尺寸。
- I/O/T 等方块占地。
- 边界和固定块碰撞。
- 幽灵落点。
- SRS 墙踢。
- 锁定写回。
- 消行和上方块下落。
- 计分、连消、软降、硬降。
- 速度档。
- 7-bag。

## 构建检查

命令：

```bash
git diff --check
./scripts/build-app.sh --stacktrace
```

结果：

- `git diff --check` 通过。
- `BUILD SUCCESSFUL`。

## 真机验收

本轮未执行真机安装和试玩。

后续真机重点：

- Canvas 棋盘是否清晰、无黑屏、尺寸正确。
- `←/→` 点按和长按是否稳定。
- `↓` 点按软降、长按硬降是否稳定。
- `↻` 顺时针旋转和墙踢是否稳定。
- Hold 卡片点按是否符合预期。
- 暂停/继续、再来一局、回大厅是否正常。
- 高等级下落时是否明显比旧版流畅。

## 遗留风险

- 真机触摸事件坐标仍需验证，尤其是父控制面板统一 `onTouch` 的命中区域。
- Canvas 在不同尺寸设备上的像素对齐和清晰度需要实机观察。
