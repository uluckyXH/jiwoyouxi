# 成就墙 Logo 与未解锁样式验证

日期：2026-09-17

测试人：Codex

分支：`main`，当前工作区

## 改动

- 保留成就墙常驻 DEV 按钮、确认 Sheet 和重置流程。
- 直接绘制 `achievement_wall_logo.svg`，在标题旁以 40vp 显示，替换原来的字符图标；不新增成就条目。
- 移除共享 `AchievementBadge` 的灰度和透明度处理。列表与详情中的未解锁图标保留原色，使用近白奶油底 / 深暖灰底与细边框；解锁仍使用暖金底、主题色描边和勾选。
- 新增未解锁底板与边框的浅深色 token，标题文字增加宽度分配及单行省略以适应新标识。

## 检查结果

| 检查 | 结果 |
| --- | --- |
| DEV 保留 | PASS：`DeveloperTools` 与重置确认区域和仓库基线逐字一致，根布局仍直接调用 `DeveloperTools()` |
| SVG 结构 | PASS：透明 128 × 128 SVG，仅使用静态路径和圆形 |
| 原图保留 | PASS：共享徽章 Image 不再设置 `grayscale` 或 `opacity`，22 枚成就 SVG 无本轮变更 |
| 离线视觉检查 | PASS：检查新 Logo 与浅深色模式下的旧未解锁、新未解锁和已解锁样式 |
| 应用构建 | PASS：DevEco `assembleApp` 输出 `BUILD SUCCESSFUL` |
| HAP 资源 | PASS：新 Logo 已打包，文件内容与当前源码一致 |
| 静态差异 | PASS：`git diff --check` |
| 设备侧显示 | 未执行；列表、详情、深浅色切换和小屏布局仍待原生设备验收 |

本轮为素材与样式调整，没有修改成就规则或存档，也没有新增规则测试。

## 构建环境

DevEco Studio 6.1.1，实际安装路径 `/Volumes/MacSSD/Applications/DevEco-Studio.app`，使用其捆绑 SDK / JBR，项目目标版本未调整。

```bash
DEVECO_STUDIO_APP=/Volumes/MacSSD/Applications/DevEco-Studio.app \
  ./scripts/build-app.sh --stacktrace
```

日志：`/tmp/jiwoyouxi-achievement-logo-build.log`。

## 视觉记录

[Logo 与未解锁样式对比](../docs/xiaohuang/成就系统/预览/成就墙Logo与未解锁样式.png)为本地 SVG / Sharp 生成的离线效果预览，并非原生应用截图。已解锁徽章沿用各游戏的主题色描边，示例中的金色勾选保持统一。
