# 扫雷成就接入测试报告

- 日期：2026-08-27
- 测试人：Codex
- 分支：`main`
- 基线提交：`b8d2502 feat: add achievement unlock notification`（本轮实现尚未提交）
- 设备：未连接真机或模拟器
- HarmonyOS 版本：未连接设备，无法读取
- DevEco Studio：`/Volumes/MacSSD/Applications/DevEco-Studio.app`

## 变更范围

- 在 `MinesweeperPage.ets` 的既有结算出口旁追加一次通用 `sessionEnd` 事实上报；没有拆分、移动、重排或修改扫雷玩法、棋盘、HUD、结果页、计时、插旗上限和分数逻辑。
- 每次重开生成独立会话；开启看雷开发功能后，该局标为非正式局；新增两项仅供上报的插旗事实：`manualAllMinesFlagged` 与 `hasEverFlaggedNonMine`。
- 在注册表启用扫雷三条已确认规则：`平安出窝`、`迅捷排雷（<= 60 秒）`、`旗开得胜（十旗全对且未曾插错）`。
- `Index.ets` 仅向扫雷注入已有的通用上报回调；游戏不导入通知组件，不包含成就 ID 或判定阈值。
- 新增 ArkTS Hypium 和 Node 纯逻辑用例；更新扫雷设计与成就系统索引状态。

## 构建与测试命令

```bash
node scripts/test-achievement-engine.mjs
DEVECO_STUDIO_APP='/Volumes/MacSSD/Applications/DevEco-Studio.app' ./scripts/build-app.sh --stacktrace
DEVECO_SDK_HOME='/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/sdk' \
  JAVA_HOME='/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/jbr/Contents/Home' \
  /Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw onDeviceTest \
  -p module=entry -p product=default -p coverage=false \
  --node-home /Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/tools/node \
  --no-daemon --stacktrace
/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/toolchains/hdc list targets -v
git diff --check
git diff --name-only -- entry/src/main/ets/games
```

## 用例结果

| 用例 | 结果 | 备注 |
| --- | --- | --- |
| 成就注册表有效性 | PASS | 扫雷事实白名单与三条启用规则匹配，`achievementRegistryIssues()` 为 0。 |
| 首局 60 秒十旗全对 | PASS | 按注册顺序解锁 `global.first_settlement`、`minesweeper.first_clear`、`minesweeper.quick_clear`、`minesweeper.perfect_flags`。 |
| 61 秒边界 | PASS | 可以解锁首通 / 十旗全对，不会解锁“迅捷排雷”。 |
| 插错后撤旗 | PASS | 即使最终十旗正确，`hasEverFlaggedNonMine = 1` 也不会解锁“旗开得胜”。 |
| 系统自动补旗 | PASS | `manualAllMinesFlagged = 0` 时不会从最终棋盘反推“旗开得胜”。 |
| DEV 看雷辅助局 | PASS | 按正常局以 `isOfficial = true` 上报；快速通关可解锁“鸡窝开张 / 平安出窝 / 迅捷排雷”，不会因自动补旗误得“旗开得胜”。 |
| 失败结算 | PASS | 正式失败局会计入合集首局统计，但不会解锁任一要求胜利的扫雷成就。 |
| Node 成就 / 通知回归 | PASS | `node scripts/test-achievement-engine.mjs` 输出 `Achievement core and notice tests passed`。仅有 Node `stripTypeScriptTypes` 实验特性提示。 |
| ArkTS 主模块构建 | PASS | 使用配置后的 DevEco Studio 路径，`CompileArkTS`、`PackageHap` 完成，输出 `BUILD SUCCESSFUL`。仅有未配置签名警告。 |
| Hypium 测试源码编译与打包 | PASS | `:entry:ohosTest@OhosTestCompileArkTS` 与 `:entry:ohosTest@PackageHap` 成功完成，新增测试进入测试 HAP。 |
| Hypium 真机执行 | BLOCKED | `onDeviceTest` 后续 `GenerateDeviceCoverage` 报 `00308018 Connect server failed`；`hdc list targets -v` 同样报 `Connect server failed`。没有将测试包编译成功误记为设备侧执行成功。 |
| 游戏目录范围检查 | PASS | `git diff --name-only -- entry/src/main/ets/games` 只输出允许接入的 `minesweeper/MinesweeperPage.ets`。 |
| 差异格式检查 | PASS | `git diff --check` 无输出。 |

## 真机待验收

- 首局普通胜利后，先出现既有结果层，约 `350ms` 后依次播放“鸡窝开张”和“平安出窝”，两张卡不重叠。
- 60 秒内完成且十旗全对时，确认前三条单项通知后再显示第 4 条的汇总通知；结果页的“再来一局”和“回大厅”仍可正常点击。
- 验证 60 秒、61 秒、插错后撤旗、未手动插旗但自动补旗、开启 DEV 看雷后通关五种实际流程；DEV 看雷局应与正常局一样参与判定。
- 验证重开、回大厅和页面销毁会清理尚未播放的成就提示计时器，且不影响既有扫雷退出、暂停、重开和计分流程。
