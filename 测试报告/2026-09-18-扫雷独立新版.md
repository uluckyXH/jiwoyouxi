# 扫雷独立新版测试报告

日期：2026-09-18

后续手机适配、音量与性能优化见本文末尾补充记录；首次交付的 9 项测试已扩展为 10 项。

## 范围与结论

「庭院探雷」在 `gamesNext/minesweeper/` 独立实现，资源放在 `rawfile/gamesNext/minesweeper/`。新版已接入首页，与经典版共用新绘制的 Logo；旧版代码、资源和存档没有改写。实现说明见 [扫雷独立重构版](../docs/xiaohuang/扫雷/扫雷独立重构版.md)。

最终应用构建、测试 HAP 构建、主机规则测试、原生 Hypium 测试、成就规则回归均通过。Pura 90、MatePad Pro 13、Mate X7 模拟器完成原生界面检查，发现的计时显示、弹窗点击、棋盘滚动与窗口重排问题已修正。以下设备均为本地模拟器，不代表真实硬件验收。

## 环境

| 环境 | 用途 |
| --- | --- |
| DevEco Studio 安装目录 `/Volumes/MacSSD/Applications/DevEco-Studio.app` | 应用和测试 HAP 构建、hdc、模拟器 |
| Node.js 24.14.0 | 直接执行 ArkTS 纯规则及主机回归 |
| Pura 90，HarmonyOS 6.1.1 / API 24，1320 × 2856 | 手机浅深色、交互、存档、横竖屏、经典版入口 |
| MatePad Pro 13，HarmonyOS 6.1.1 / API 24，2880 × 1920 | 大屏布局、原生 Hypium 测试 |
| Mate X7，HarmonyOS 6.1.1 / API 24 | 展开 2210 × 2416、收起 1080 × 2444、半折叠 |

保持项目现有签名配置。测试使用本地构建的 unsigned HAP 安装至模拟器，没有新增或修改签名材料。

## 自动验证

| 项目 | 最终结果 |
| --- | --- |
| `scripts/build-app.sh --stacktrace` | `BUILD SUCCESSFUL` |
| `entry@ohosTest assembleHap` | `BUILD SUCCESSFUL` |
| `node scripts/test-mine-garden-next.mjs` | 9 组规则及布局测试通过，素材与隔离检查通过 |
| 原生 `MineGardenNext` Hypium | `Tests run: 9, Failure: 0, Error: 0, Pass: 9, Ignore: 0`；退出结果码 0 |
| `node scripts/test-achievement-engine.mjs` | 通过 |
| `git diff --check` | 通过 |
| 旧 `games/` 与 `rawfile/games/` 差异检查 | 无改动 |
| 121 个 SVG、8 个 WAV | SVG 可解析，无外链／脚本；WAV 文件头与音频参数有效 |

`MineGardenNext.test.ets` 同时由主机脚本和原生 Hypium 执行，覆盖：

1. 三档难度共 90 个随机种子棋盘的雷数、首点九宫格安全及邻雷计数。
2. 开局前插旗不布雷、不计时；旗数上限和非法位置输入。
3. 空白展开保留旗帜；打开全部安全格获胜；误插旗历史。
4. 数字连开的成功路径与插错旗后触雷路径。
5. 终局输入与计时锁定、手动旗与自动补旗区分、得分和分档最佳用时。
6. 随机状态、旗帜、用时和终局存档恢复。
7. 非法和不一致存档原子拒绝，不污染当前棋盘。
8. 8 种窗口尺寸及横向／纵向折痕的触控尺寸、布局边界。
9. 新旧入口共用 Logo，收藏计数归属同一逻辑游戏。

原生测试首次运行暴露已有 `OpenHarmonyTestRunner.ets` 在成功回调收到 `null` 时读取 `error.code` 的问题。增加一处 `error !== null` 判断后重新构建测试包，最终 9 项全部通过。该修改只作用于测试运行器。

应用构建保留 `MineStorage.load()` 两处可抛异常的编译提示；调用方 `boot()` 已用 `try/catch` 转入读取失败界面。没有将构建成功描述为零警告。

## 原生界面实测

| 场景 | 操作与观察 |
| --- | --- |
| 新旧入口 | 首页显示「新版 · 庭院探雷」和「经典版」，Logo 一致；分别进入不同实现 |
| 经典版回归 | 可进入原「鸡雷」界面，显示原有最高分并可返回大厅；未出现新版场景 |
| 浅深色 | App 外观切换后，新版场景、格子、控件、图标和弹窗跟随；深色系统栏文字清晰 |
| 翻格与长按 | 首次点按展开安全区；长按插旗后旗数为 09，未意外翻开该格 |
| 计时与暂停 | 游戏中时钟持续更新；暂停后两次读取均为 03:16；继续后恢复计时 |
| 难度切换 | 已有进度时先确认；取消保留原局，确认后进阶为 0 / 105、25 雷，挑战为 0 / 147、45 雷 |
| 大棋盘拖动 | 挑战棋盘横向、纵向均能拖动，结构中的棋盘位置随之改变；翻格数仍为 0 / 147，旗数仍为 45 |
| 存档恢复 | 保存返回后重新进入，显示继续提示；恢复到 4 / 54、09 面可用旗、01:31，与退出前一致 |
| 手机旋转 | 1320 × 2856 切换至 2856 × 1320 再还原；同一局的 11 / 105 与棋盘保持一致 |
| 平板布局 | 棋盘与侧栏并排；卡片按棋盘收紧；挑战难度的 16 行可完整显示 |
| 折叠切换 | Mate X7 展开→收起→展开，已翻开格数始终 17 / 54，未重新开局 |
| 半折叠 | 使用折痕下方较大的区域；棋盘与侧栏可滚动；最终版本时钟和难度文字完整 |
| 触雷结算 | 出现「小心，踩到雷了」结果弹窗；可关闭弹窗复盘雷区 |

界面验证中修正了以下实际问题：

- 将计时、余旗等从按值传入的 Builder 改为响应状态的独立组件，确保实时更新。
- 弹窗使用允许子组件接收点击的命中策略，修复操作按钮无法点击。
- 棋盘采用纵向与横向 Scroll 组合，修复大棋盘拖动及格子区域尺寸问题。
- 棋盘卡片尺寸直接依赖当前布局状态，窗口变化后重新计算，保留同一局数据。
- 深色模式同步系统栏文字颜色；退出恢复原窗口设置。
- 半折叠扩大侧栏最小宽度并调整数字字号，防止时钟截断；难度按钮去除多余默认内边距。

## 验证命令

设置实际 DevEco 路径后运行应用构建：

```sh
DEVECO_STUDIO_APP=/Volumes/MacSSD/Applications/DevEco-Studio.app ./scripts/build-app.sh --stacktrace
node scripts/test-mine-garden-next.mjs
node scripts/test-achievement-engine.mjs
git diff --check
```

测试包使用 `entry@ohosTest` 构建任务。安装应用 HAP 与测试 HAP 后，在已连接模拟器执行：

```sh
hdc -t <设备地址> shell aa test -b com.longxin.jiwoyouxi -m entry_test -s unittest OpenHarmonyTestRunner -s class MineGardenNext -w 60
```

## 本地证据与待验范围

截图、选取的原生界面结构、构建日志和 Hypium 最终日志放在本地忽略目录 `phone-shots/minesweeper-next/`，未将二进制截图加入版本控制：

- `mine-final-light.jpeg` / `mine-final-dark.jpeg`：手机浅深色。
- `mine-landscape.jpeg` / `mine-final-tablet.jpeg`：手机横屏、平板。
- `mine-fold-closed.jpeg` / `mine-fold-reopened.jpeg` / `mine-fold-half-final.jpeg`：折叠形态。
- `mine-loss-dialog.jpeg`：触雷结算。
- `mine-native-test.log`：原生 9 / 9 测试结果。

本轮未完成的实机验收范围：真实设备折痕和安全区、分屏拖拽与系统字体放大、音效实际外放／耳机听感、长时间游玩及系统强杀恢复。8 个原创 WAV 和 SoundPool 接入已完成，未将文件格式检查当作听感验证。胜利判定、得分、手动旗和存档已由原生规则测试覆盖；完整胜利弹窗及成就通知的端到端游玩仍需补充人工验收。

## 同日补充：手机适配、音量与性能

实施与性能数据见 [手机适配与性能评估](../docs/xiaohuang/扫雷/手机适配与性能评估.md)。

- 手机按难度缩小格子，正常手机及收起外屏优先整盘显示；极矮窗口保留必要的纵向浏览。大屏继续原尺寸算法。
- 半折叠与空间不足按实际溢出启用横纵滚动，不自动遮蔽或要求切换姿态。布局测试明确断言半折叠保留两轴浏览。
- Mate X7 半折叠原生复核：棋盘原始边界从 `[115,1656][1615,3656]` 横拖到 `[-255,1656][1245,3656]`，再纵拖到 `[-255,1311][1245,3311]`。进度保持 4 / 147，未误触翻格，也没有姿态限制弹窗。
- Pura 90 原生画面检查了挑战棋盘；Mate X7 收起形态逐档核对 64、130、192 个格子可见（允许原生像素取整的 1px 差异）。最终挑战档底部提示也完整显示，展开后同一局仍为 4 / 147。
- 长按插旗余旗变为 44、已翻开仍为 0；撤旗并首次点开后变为 4 / 147、00:02。暂停停在 01:05，继续后为 01:07；后台返回保持暂停。
- 应用与测试 HAP 构建通过；主机规则／布局测试和原生 Hypium 均为 10 / 10 通过；成就规则回归通过。
- 音效数字电平合计提高约 9.33 dB。8 个 WAV 的峰值检查通过，无素材削波；真机听感待验。
- 主机挑战档首次布雷与展开 P95 为 0.1057ms；复制状态、统计与序列化 P95 为 0.0488ms。该基准不包含原生绘制和磁盘刷新。
- 优化计时为仅在可操作对局中每秒刷新，暂停、后台、终局不保留计时器；无效操作不立即写盘，结算去除重复保存。

本轮证据使用 `mine-fit-*`、`mine-perf-baseline.json` 命名，保留于本地 `phone-shots/minesweeper-next/`。三折叠尺寸的新旧公式一致性由布局测试覆盖，本轮没有完成三折叠全部物理形态的原生验收。

## 同日追加：留白、背景与布局过渡（代码验收）

用户明确要求本次不查看页面，由用户自行验收；此次只进行源码、规则、资源和构建检查。

- 手机控件收紧，卡片按实际棋盘宽高贴合；390 × 844vp、顶部／底部安全区 28／20vp 的进阶与挑战格子由 32／26vp 增至 34／28vp。常规按钮仍为 44vp，矮窗口及半折叠保留滚动。
- 背景改为四层边角定位 SVG，浅深色素材分别绘制；全套 127 个 SVG 通过 XML 解析与 viewBox 检查。
- 有效布局变化增加 200ms 原生过渡；相同窗口通知直接跳过，首帧不播放重排动画。没有修改规则引擎、存档、音效或其他游戏。
- `scripts/test-mine-garden-next.mjs`：11 / 11 主机用例通过，含手机完整显示、矮屏滚动、重复通知过滤、平板／展开／三折叠原公式一致性和半折叠横纵滚动；资源及音频峰值检查通过。
- 应用构建通过；保留已有 preferences 异常提示及未配置签名提示，没有新增扫雷编译错误。
- 共享工作区的测试 HAP 构建因另一位 Agent 正在修改的 `FruitMarketNext.test.ets` 第 389、404 行对象类型错误失败。随后使用 `15c8264` 的临时副本，仅叠加本轮扫雷源码、资源及测试，`entry@ohosTest` 构建通过。未覆盖、暂存或修复水果摊文件。
- `git diff --check` 通过。本轮未运行原生 Hypium，也未进行截图、触摸和动画流畅度检查；以上历史原生结果不代表新画面的验收结论。

本次构建日志位于本机 `/tmp/mine-polish-build.log`、`/tmp/mine-polish-test-build.log`（共享目录失败记录）及 `/tmp/mine-polish-test-isolated.log`（独立副本通过记录），不加入版本控制。

## 背景分界线修正

用户指出展开大屏中穿过提示区域的背景色块分界。浅深色地面 SVG 改为显式 `userSpaceOnUse` 坐标，渐变覆盖完整 240 单位画布高度；通过多个透明度节点平滑过渡，上下边缘均为全透明，最大不透明度由 0.90 降为 0.44，柔化背景色块与安全区交界。生成脚本同步更新。

两张 SVG 的 XML、渐变范围、节点顺序及首末透明度检查通过；应用构建通过，保留原有未配置签名提示。`git diff --check` 通过。本次仅调整静态背景资源与说明，未新增测试用例；按用户要求不打开页面，实际视觉由用户验收。构建日志：`/tmp/mine-background-seam-build.log`。
