# 2048 · 暖光叠数独立新版验证

日期：2026-09-18。实施说明：[2048 独立重构版](../docs/xiaohuang/2048/2048独立重构版.md)。

## 范围

新版 `gamesNext/glow2048/`、对应原生 SVG／WAV 资源、首页新入口 `chicken2048Next`、逻辑游戏 `chicken2048` 的成就与收藏映射。经典版源码、旧资源及既有 Logo 没有修改。入口共同指向 `app/game_logos/game_icon_chicken2048.svg`。

## 通过的检查

- `scripts/test-glow-2048.mjs`：13 项规则、布局与集成用例通过。覆盖初始双格、四方向、同回合不得二次合并、输入不变性、总值守恒、唯一格位、完整移动路径、无效方向不生成、不改变随机序列、有效移动只生成一格、得分、满盘可移动性、2048 后继续、连合不加倍率、确定性恢复、损坏存档拒绝、手机／平板／折叠尺寸、旧 Logo 与收藏去重。
- `scripts/test-glow-2048-lifecycle.mjs`：10 项控制逻辑用例通过。直接加载页面字段和非 UI 方法，仅替换平台边界，不复制页面实现。覆盖快速连续输入、旧回调失效、滑动中暂停／旋转／后台、动画完成回调丢失、2048 被暂停打断、重开不被旧回调污染、退出保存失败重试、恢复与读取失败、离开释放资源。
- `scripts/test-achievement-engine.mjs`：成就核心与通知回归通过。
- 132 个 SVG 的 XML 和 viewBox 检查通过，无脚本或嵌入位图。8 个 WAV 文件头、采样格式与峰值检查通过；24kHz／16bit／单声道，峰值 0.202–0.504，没有素材削波。
- 应用 `assembleApp` 与 `entry@ohosTest assembleHap` 编译通过。保留 preferences API 的异常处理提示（读取异常由页面捕获）和项目既有未配置签名提示。
- `git diff --check` 通过，经典版 2048 源码、资源与 Logo 差异为空。

共享工作区还有其他任务修改首页和俄罗斯方块。本轮另创建 `ddd7731` 的临时副本，只叠加 2048 新文件和五处必要入口／测试接入，独立构建应用及测试 HAP，并复跑上述主机测试，均通过。提交范围仅取 2048 自己的公共层改动，不整体暂存其他任务的共享文件。

## 测试命令与日志

```sh
node scripts/test-glow-2048.mjs
node scripts/test-glow-2048-lifecycle.mjs
node scripts/test-achievement-engine.mjs
DEVECO_STUDIO_APP=/Volumes/MacSSD/Applications/DevEco-Studio.app ./scripts/build-app.sh --stacktrace
git diff --check
```

Node 使用 24.14.0。SDK 为本地 DevEco Studio 配套版本。原始构建日志位于 `/tmp/glow-2048-build.log`、`/tmp/glow-2048-test-build.log`、`/tmp/glow-2048-isolated-app.log` 和 `/tmp/glow-2048-isolated-test.log`，不提交二进制构建产物或临时日志。

## 验收边界

按用户要求，本轮未打开页面、启动模拟器或执行原生 Hypium。主机生命周期测试不能验证 ArkUI 绘制、触摸命中和真实手势仲裁；构建通过不等于视觉验收。请在设备上重点查看滑动与合并衔接、快速输入、2048 弹窗、手机横竖屏、展开／收起／半折叠、系统字体和安全区、声音开关及外放听感。布局变化的棋盘连续性已有控制逻辑覆盖，真实折痕与多折设备仍以用户实际查看为准。

## 后续修复：方块视图与操作区

用户截图揭示初版的测试盲区：引擎最大数字、得分已经变化，普通对象配合稳定 ID 的 `ForEach` 却留下旧的方块视图。初版主机测试只验证控制器的数据快照，不代表实际组件已经重绘。

本次改为持有稳定 `@Observed` 方块对象，棋盘通过 `@Link` 接收数组，每个方块以 `@ObjectLink` 读取当前值与位置。编译产物确认对象同步属性、数字读取、坐标读取与变化监听均已生成。规则引擎无需改变。

本轮验证：

- `test-glow-2048.mjs`：15 项通过，新增向下跨空位压缩／一次合并、滑行和结算两阶段对象复用、数字及位置更新、输入快照不被视图修改、生成与新开局身份检查。
- `test-glow-2048-lifecycle.mjs`：11 项通过，新增保留原方块视图引用后，连续完成向下移动及合并，校验 2→4、4→8、位置、得分和最大数字同步。
- 更新手机布局高度预算为棋盘之外 368vp，覆盖大十字方向盘；正常手机无须缩小按钮，短窗口允许滚动。
- 142 个 SVG 共 51,860 字节，XML／viewBox 与透明渐变端点检查通过。新增浅深色窗光、植物、静物、氛围和方向盘纹章；没有位图、脚本或常驻动画。8 个既有 WAV 未改动。
- 基于 `519f7ea` 加本次修改的 `/tmp/glow-polish-check-ayuh8ga4`，应用与测试 HAP 编译通过；上述两组主机测试在副本中也通过。日志为 `/tmp/glow-polish-isolated-app.log`、`/tmp/glow-polish-isolated-test.log`。
- 共享工作区首次应用编译发现本次新增组件的保留属性名冲突，已更名修复；另有其他任务的 `HubHeroBanner.objectPosition` 错误，未修改该文件，因此采用独立副本验证本次提交。

未打开页面、模拟器或执行原生 Hypium。新增回归覆盖视图模型和控制器，无法替代实际 ArkUI 绘制、手势与触控检查；用户重点查看向下合并后的数字／位置、十字按钮的手感、背景在浅深色与折叠／平板窗口的呈现。
