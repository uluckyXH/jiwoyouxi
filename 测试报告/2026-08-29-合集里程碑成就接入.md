# 合集里程碑成就接入测试报告

日期：2026-08-29
测试人：Codex
分支：`main`
变更范围：六款主推游戏的正式结算上报、收藏家应用事件、事件串行持久化、Hypium 规则测试与成就文档回写。

## 验证结果

| 项目 | 命令或方式 | 结果 |
| --- | --- | --- |
| 应用 ArkTS 构建 | `./scripts/build-app.sh --stacktrace`，使用项目 DevEco 工具链 | 通过，`BUILD SUCCESSFUL`。 |
| Hypium 测试模块编译 | `hvigorw --mode module -p module=entry@ohosTest assembleHap --no-daemon --stacktrace`，使用项目 DevEco SDK 与 JBR | 通过，`OhosTestCompileArkTS` 与 `PackageHap` 完成。 |
| 静态差异检查 | `git diff --check` | 通过。 |
| 设备发现 | DevEco SDK `hdc list targets` | 未发现已连接真机或模拟器，未执行设备侧 Hypium。 |

## Hypium 覆盖

- `AchievementEngine.test.ets`：正式结算累计、六个主推游戏去重、十局阈值、重复事件、非正式事件、全局收藏事件和非法 `global sessionEnd`。
- `AchievementFavoriteEvent.test.ets`：有效主推收藏计数、成功写入后生成事件、写入失败恢复原快照、取消收藏和非主推收藏不生成事件。
- `List.test.ets`：已登记新测试套件，随 `ohosTest` 模块编译。

## 待真机验收

当前环境没有可用 `hdc` 目标，以下流程必须在连接设备后验证：

1. 六款主推游戏分别完成一局自然结果，确认全局局数和 `6 / 6` 去重进度。
2. 每款游戏验证重开、主动退出、系统返回不会增加局数；乱斗玩家干预赛与 2048 开发摆盘不产生永久进度。
3. 连续收藏三款主推游戏，确认收藏家静默解锁；取消收藏后已解锁状态不回退。
4. 确认结算页、底部成就提示、手势区域、音频和最高分原有行为不受影响。

本报告不以测试模块编译替代真机结果；设备侧执行完成后应补充设备型号、HarmonyOS 版本、日志与实际结论。
