# 2026-06-11 合成大西瓜水果素材接入测试报告

## 测试范围

- 接入 `entry/src/main/resources/rawfile/games/suika/fruits/` 下 11 张水果 PNG。
- 水果主体从 Canvas 程序圆形改为 Canvas `drawImage(PixelMap)` 绘制。
- 保留 Canvas 同帧绘制背景、容器、失败线、合成反馈、水果阴影和水果贴图。
- 移除移动水果的 ArkUI `Image` 组件层，避免图片位置和物理循环不同步。
- 图片解码失败时回退到 Canvas 程序圆形水果，保证核心玩法不中断。
- 玩法碰撞半径、计分、生成序列不变。

## 自动化测试

```bash
node --no-warnings scripts/test-suika-rules.mjs
```

结果：通过。

```bash
node --no-warnings scripts/test-egg-sling-physics.mjs
```

结果：通过。

## 格式检查

```bash
git diff --check
```

结果：通过。

## 构建检查

```bash
./scripts/build-app.sh --stacktrace
```

结果：通过。

备注：构建输出中仍有 `No signingConfig found for product default` 警告，属于当前本地默认签名配置状态，不影响本次 ArkTS 编译和打包通过。

## 待真机验证

- 水果图片大小是否和碰撞体视觉边界一致。
- 小水果预览是否清晰。
- 大水果堆叠时图片是否遮挡失败线和分数浮动。
- 下落时贴图是否跟随阴影和碰撞体连续移动。
- `水果切图.png` 当前未接入，后续建议移出 rawfile 或转入文档目录，避免作为运行资源打包。
