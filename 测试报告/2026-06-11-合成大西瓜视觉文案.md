# 2026-06-11 合成大西瓜视觉文案测试报告

## 测试范围

- 鸡窝水果摊视觉方向文档落地。
- 游戏标题、分数板、失败文案、暂停文案、终局合成文案调整。
- 水果等级显示名调整。
- Canvas 临时半眯眼表情占位。
- 大厅入口、首页标签和成就文案调整。

## 自动化测试

```bash
node --no-warnings scripts/test-suika-rules.mjs
```

结果：通过。

说明：本次没有修改玩法规则，规则测试用于确认计分、生成、合成和失败线逻辑未受文案调整影响。

## 格式检查

```bash
git diff --check
```

结果：通过。

## 构建检查

```bash
./scripts/build-app.sh --stacktrace
```

结果：通过。

备注：构建输出中仍有 `No signingConfig found for product default` 警告，属于当前本地默认签名配置状态，不影响本次 ArkTS 编译和打包通过。

## 待真机验证

- 顶部 `今日瓜分` 文案在小屏上是否被正确省略。
- 水果内部短标签是否不会挤出圆形。
- 半眯眼表情在小等级水果上是否清晰。
- 「鸡窝被塞满了！」结算弹窗是否居中且不遮挡按钮。
