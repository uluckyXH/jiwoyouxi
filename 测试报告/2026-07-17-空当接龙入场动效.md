# 空当接龙入场动效测试报告

测试日期：2026-07-17

测试人：Codex

分支：`main`

变更范围：空当接龙资源就绪、场景淡入、七轮发牌、跳过发牌和 HUD 拆分。

## 原生测试用例

已新增 HarmonyOS 官方 Hypium / ArkXTest 测试：

`entry/src/ohosTest/ets/test/FreecellIntroRules.test.ets`

覆盖范围：

- 6 张、7 张牌列混排时的最后发牌轮次。
- 资源准备、发牌中、完成后的牌面可见性。
- 只有 `READY` 阶段开放牌桌操作。
- 关键资源重复回调、未知回调和失败回调的兜底行为。

## 构建与执行

按开发规范执行的构建命令：

```bash
./scripts/build-app.sh --stacktrace
```

结果：未执行到 hvigor 构建阶段，环境阻塞。

```text
./scripts/build-app.sh: line 13:
/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw:
No such file or directory
```

当前 `/Applications/DevEco-Studio.app` 存在，但其 `Contents/` 下没有 `tools/` 目录，因此无法执行 Hypium 用例、打包 HAP 或安装真机。

## 真机验收

未执行。等待可用 DevEco / hvigor 环境后，按以下顺序验收：

1. 冷启动和二次进入空当接龙，无白屏、黑屏或布局跳变。
2. 关键资源及时就绪时，确认背景与 HUD 轻入、牌桌按 7 轮建立。
3. 资源慢或失败时，确认直接静态进入，不停留在不可操作状态。
4. 发牌中轻触牌桌，确认立即完整展牌且不产生选牌或移动。
5. 发牌中点击大厅或系统返回，确认无残留定时器和页面离开后的状态更新。
6. 新局、撤销、提示、自动归位、结算、最高分和小屏布局与改动前一致。

## 结论

Hypium 测试用例已写入工程，但尚未在 HarmonyOS 工具链中执行。当前不得标记为“构建通过”或“真机验收通过”；阻塞条件是本机 DevEco 安装缺少 hvigor 工具。
