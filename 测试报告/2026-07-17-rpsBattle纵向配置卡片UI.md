# 石头剪刀布大作战纵向配置卡片 UI 测试报告

日期：2026-07-17
范围：新版 `rpsBattle` 开局配置页纵向卡片重排、固定开始动作区和 UI 模块拆分
状态：规则回归通过；ArkTS 构建与真机视觉验收待补

## 变更范围

- 开局配置改为：玩法方案、按模式显示的本局机制、阵营主题、支持阵营、战斗规模、对局时长。
- 团队占点保留中心据点锁定和黑洞开关；经典乱斗不显示空机制卡。
- 高级人数调整收进战斗规模卡，三方计数器改为纵向行布局，按钮热区为 `40vp x 40vp`。
- 新增固定底部“开始乱斗”动作区，滚动内容为其预留底部空间。
- 仅重组 ArkUI 界面和组件边界；未修改规则、引擎、Canvas、音频或模式默认值。

## 自动回归

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `node scripts/test-rps-battle-lineup-rules.mjs` | 通过，11 项 | 人数上下限、同步/独立调整、模式默认机制与本局关闭规则保持正确 |
| `node scripts/test-rps-battle-zone-system.mjs` | 通过，6 项 | 中心据点生成、占领、僵持、结算与快照隔离保持正确 |
| `node scripts/test-rps-battle-black-hole-system.mjs` | 通过，9 项 | 黑洞激活、吸引、吞噬、动态上限、据点避让与快照隔离保持正确 |
| `git diff --check`（本轮 RPS 文件） | 通过 | 未发现空白或补丁格式错误 |

说明：前三项是仓库既有的纯规则回归脚本，仅用于验证 UI 重排没有影响现有行为；本次未新增 Node 运行时代码，也没有在 HarmonyOS 页面中引入 Node 依赖。

## ArkTS 构建

已执行：

```bash
./scripts/build-app.sh --stacktrace
```

结果：未能进入 ArkTS 编译阶段，工作区的构建脚本引用了不存在的本机文件：

```text
/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw
```

已检查 `/Applications` 与仓库目录，未发现可替代的 `hvigorw`。该结果是本机构建环境缺失，不代表本次 ArkTS 代码已通过编译。

## 待真机验收

在可用 DevEco / hvigor 环境中完成构建后，需在小屏和常规手机屏验证：

1. 选择经典乱斗：不出现空的“本局机制”卡，开始乱斗、倒计时、暂停、重开与结算正常。
2. 选择团队占点：中心据点显示核心锁定；黑洞默认开启，关闭后固定底栏摘要改为“黑洞关”，开局不出现黑洞。
3. 切换主题、支持阵营、规模、时长和高级人数：选择状态正确保留，切换模式只重置机制默认值。
4. 展开高级人数：一起调和单独调正确切换，三方加减不超过每方 `20`、总数 `60`，小屏不重叠或溢出。
5. 滚到最底部：时长卡完整可见，固定“开始乱斗”栏不遮挡内容且避开系统手势区。
6. 大局团队占点运行后检查 `RpsBattlePerf`，确认 UI 重排未改变 `update`、`draw`、`zoneMs`、`blackHoleMs` 的运行时表现。

## 结论

规则回归没有发现行为退化。由于本机没有可用 hvigor，ArkTS 编译和真机视觉体验仍需要在开发设备上补验；在此之前不应把本次 UI 改造标记为已完全验收。
