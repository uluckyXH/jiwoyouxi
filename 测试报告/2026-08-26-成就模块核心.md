# 成就模块核心 测试报告

- 日期：2026-08-26
- 测试人：Codex
- 分支/提交：当前工作区，基线 `c2e92db`，本轮未提交
- 设备：未连接真机或模拟器
- HarmonyOS 版本：未连接设备，无法读取
- DevEco Studio：本机 `/Volumes/MacSSD/Applications/DevEco-Studio.app`

## 变更范围

- 新增独立的 `AchievementEvent`、`AchievementState`、规则注册表、纯判定引擎与应用级 `AchievementService`。
- `CoopStorage` 在现有 Preferences 文件内使用独立键 `achievement_state` 保存成就状态；不读取旧游戏记录、最高分、收藏或旧成就展示数据。
- 补充 Node 纯规则回归和 HarmonyOS Hypium 用例。
- 未修改 `entry/src/main/ets/games/**`、`Index.ets`、游戏玩法、页面布局或游戏内提示。

## 构建与测试命令

```bash
node scripts/test-achievement-engine.mjs
DEVECO_STUDIO_APP='/Volumes/MacSSD/Applications/DevEco-Studio.app' ./scripts/build-app.sh --stacktrace
DEVECO_SDK_HOME='/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/sdk' \
  JAVA_HOME='/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/jbr/Contents/Home' \
  /Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw onDeviceTest \
  -p module=entry -p product=default -p coverage=false \
  --node-home /Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/tools/node \
  --no-daemon --stacktrace
git diff --check
git diff --name-only -- entry/src/main/ets/games
git status --short -- entry/src/main/ets/games
```

## 用例结果

| 用例 | 结果 | 备注 |
| --- | --- | --- |
| 成就注册表有效性 | PASS | 22 条定义、6 个主游戏档案与事实白名单均可读取。 |
| 空状态与独立状态初始化 | PASS | 空状态不依赖 `GameRecord`、分数或收藏。 |
| 阈值与高水位进度 | PASS | 2048 的 `128 → 2048 → 128` 不会回退，首次达到阈值只解锁一次。 |
| `eventId` 去重 | PASS | 同一事件再次提交返回 `duplicate`，不重复增加进度或解锁。 |
| 正式局、非正式局与非法事件 | PASS | 非正式局、未知游戏、非有限数值的已注册事实均不会产生永久解锁；未知事实键会被丢弃。 |
| 全局成就 | PASS | 1 局、10 局和已体验 6 款游戏使用模块自身状态统计。 |
| 禁用规则 | PASS | 收藏成就在没有独立大厅事件之前保持禁用。 |
| Node 纯规则回归 | PASS | `node scripts/test-achievement-engine.mjs` 通过。Node 仅提示 TypeScript strip-types 为实验特性。 |
| ArkTS 主模块构建 | PASS | `./scripts/build-app.sh --stacktrace` 输出 `BUILD SUCCESSFUL`。仅有未配置签名的 unsigned HAP 警告。 |
| Hypium 测试源码编译与打包 | PASS | `:entry:ohosTest@OhosTestCompileArkTS` 和 `:entry:ohosTest@PackageHap` 均通过。 |
| Hypium 真机执行 | BLOCKED | 后续设备覆盖任务报 `00308018 Connect server failed`；本机没有可连通的设备服务，不能将编译通过误报为测试执行通过。 |
| 真机验收 | BLOCKED | 本阶段没有接入游戏或 UI，且没有连接设备；待阶段 B/逐游戏接入时补充。 |
| 游戏目录范围检查 | PASS | `git diff --name-only -- entry/src/main/ets/games` 无输出。 |
| 差异格式检查 | PASS | `git diff --check` 无输出。 |

## 遗留与下一步

- 恢复可用 HDC/设备服务后，重新执行 `onDeviceTest`，验证 Hypium 用例的实际运行结果。
- 当前没有游戏会提交 `AchievementEvent`，也不会出现解锁提示；这是本轮“只落独立核心、不动游戏”的预期结果。
- 若进入下一阶段，只在 `Index.ets` 接通统一网关；之后由项目负责人指定第一款游戏和明确的上报位置。
