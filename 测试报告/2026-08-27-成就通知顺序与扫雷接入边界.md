# 成就通知顺序与扫雷接入边界 测试报告

- 日期：2026-08-27
- 测试人：Codex
- 基线提交：`607041b feat: connect achievement wall to live state`（当前工作区含未提交的成就提醒底座）
- 设备：未连接真机或模拟器
- HarmonyOS 版本：未连接设备，无法读取
- DevEco Studio：本机 `/Volumes/MacSSD/Applications/DevEco-Studio.app`

## 变更范围

- 在 `AchievementNotice.test.ets` 和 Node 成就回归中增加顺序用例：`global.first_settlement` 与 `minesweeper.first_clear` 必须按输入 / 解锁顺序生成通知项。
- 在扫雷成就设计中确认：同局多项成就严格串行显示，上一张卡完整出场并卸载后才显示下一张。
- 记录扫雷接入边界：当前 `games/minesweeper/**` 零差异；未来只允许在明确结算出口追加成就事实上报，禁止重构、拆分、移动或改写现有游戏逻辑。
- 未修改任何游戏文件、玩法、棋盘算法、HUD、结果页或分数逻辑。

## 构建与测试命令

```bash
node scripts/test-achievement-engine.mjs
DEVECO_SDK_HOME='/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/sdk' \
  JAVA_HOME='/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/jbr/Contents/Home' \
  /Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw onDeviceTest \
  -p module=entry -p product=default -p coverage=false \
  --node-home /Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/tools/node \
  --no-daemon --stacktrace
/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/toolchains/hdc list targets -v
git diff --check
git diff --name-only -- entry/src/main/ets/games
git status --short -- entry/src/main/ets/games
```

## 用例结果

| 用例 | 结果 | 备注 |
| --- | --- | --- |
| 两项通知的原始顺序 | PASS | `[global.first_settlement, minesweeper.first_clear]` 生成同一顺序的两条通知数据。 |
| 多项通知折叠 | PASS | 前 3 条保持原顺序；第 4 条以后生成并排在其后的汇总项。 |
| Node 成就 / 通知回归 | PASS | 输出 `Achievement core and notice tests passed`。 |
| Hypium 测试源码编译与打包 | PASS | `:entry:ohosTest@OhosTestCompileArkTS` 和 `:entry:ohosTest@PackageHap` 成功完成，新顺序用例已进入测试包。 |
| Hypium 真机执行 | BLOCKED | `hdc list targets -v` 报 `Connect server failed`；测试包构建成功不等同于设备侧用例已运行。 |
| 游戏目录范围检查 | PASS | `git diff --name-only -- entry/src/main/ets/games` 与 `git status --short -- entry/src/main/ets/games` 均无输出。 |
| 差异格式检查 | PASS | `git diff --check` 无输出。 |

## 后续真机验收

- 扫雷首局正式胜利同时解锁合集首局与扫雷首通时，确认两张通知无重叠地依次播放。
- 在结果层入场稳定后再播放通知，确认不遮挡“再来一局”和“回大厅”。
- 验证重开、离开大厅和应用离开时，未播放通知被清空但已保存成就不丢失。
