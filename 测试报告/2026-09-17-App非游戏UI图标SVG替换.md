# App 非游戏 UI 图标 SVG 替换验证

日期：2026-09-17

测试人：Codex

范围：当前工作区的非游戏 UI 图标替换

## 变更

- 新增 23 个手写 SVG：11 个功能图标、6 个页面插画图标、6 个大厅游戏 Logo。
- 替换大厅、收藏、底部导航、设置、成就分组与空状态、成就完成标记和缺省通知图标；入口胶囊通过公共资源映射复用新 Logo。
- `AppIcon` 为小图标提供主题着色；入口图标拆出 `HubGameLogoTile`，收藏与箭头点击区域为 40vp。
- 保留成就 DEV 区域、已有 22 枚勋章、游戏代码 / 游戏素材、首页横幅和系统桌面 / 启动图标。

## 环境与命令

DevEco Studio 6.1.1，实际安装路径 `/Volumes/MacSSD/Applications/DevEco-Studio.app`，使用自带 JBR 与 SDK。项目目标 API 未变更。没有连接的设备，未执行设备侧 UI 验收；没有设备型号、HarmonyOS 实机版本或原生截图可记录。

```bash
DEVECO_STUDIO_APP=/Volumes/MacSSD/Applications/DevEco-Studio.app \
  ./scripts/build-app.sh --stacktrace
git diff --check
```

构建日志：`/tmp/jiwoyouxi-app-ui-svg-build.log`。应用为未签名构建产物；未安装 / 发布。

## 结果

| 检查 | 结果 |
| --- | --- |
| 应用 ArkTS 编译与打包 | PASS，最终 `BUILD SUCCESSFUL` |
| SVG 基础结构 | PASS，46 个 App SVG 均为静态基本图形，有 viewBox，无外链 / 文本字体 / 嵌入位图 |
| HAP 资源逐字核对 | PASS，23 个新 SVG 与 23 个已有成就 SVG 全部包含且与源码一致 |
| 新 SVG 资源体积 | 12,876 字节，23 个文件合计（未压缩） |
| 图标遗漏检查 | PASS，当前非游戏页面、成就组件、入口胶囊无系统 SymbolGlyph、Emoji / 字符图标渲染；大厅仍引用的 PNG 为横幅插画 |
| 游戏改动边界 | PASS，`ets/games/` 与 `rawfile/games/` 的 git diff 为空，游戏没有引用 `gameLogoPath` |
| 深浅色离线预览 | PASS，已查看全部新素材在两种底色下的效果，功能图标以 24px 预览 |
| 差异格式检查 | PASS，`git diff --check` |
| 原生 SVG 着色 / 手势 / 布局 | 未执行，等待设备验收 |

初次编译发现自定义属性 `size` 与 ArkUI 公共方法冲突，以及 `ClickEvent` 没有 `stopPropagation`；已将属性命名为 `iconSize` 并沿用原有 `onClick` 回调形式，最终构建通过。

此次为素材和 UI 调整，没有修改玩法、成就规则或存储，没有新增规则单测。离线预览只核验资源外观，不替代原生 UI 自动化或真机测试。

## 视觉记录与待验收

[App 非游戏 UI 图标预览](../docs/xiaohuang/预览/App非游戏UI图标SVG.png)由实际 SVG 资源离线合成，不是原生应用截图。

设备可用后检查：

1. 四个底部 Tab 正常切换，图标和文字对齐，选中状态清楚。
2. 大厅按钮和设置里的深浅色切换同步更新图标着色。
3. 收藏星标点击只更新收藏，点击卡片进入游戏；收藏页与空状态均正常。
4. 成就全部 / 已解锁 / 进行中筛选、详情清单和解锁勾选显示正常；DEV 按钮仍可见。
5. App 进入身份胶囊与成就通知无文字或图标裁切。
6. 小屏和常规手机尺寸下，统计卡片、双列游戏入口与底部安全区无重叠。
