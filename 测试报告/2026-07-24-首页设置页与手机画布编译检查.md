# 首页设置页与手机画布编译检查

- 日期：2026-07-24
- 测试人：Codex
- 分支/提交：当前工作区未提交改动
- 设备：未连接真机
- HarmonyOS 版本：未真机验证
- DevEco Studio：`/Volumes/MacSSD/Applications/DevEco-Studio.app`

## 变更范围

- 大厅与已接入小游戏在平板、折叠屏中使用成对计算的手机画布。
- 首页滚动区域隐藏可视滚动条。
- 鸡窝水果摊外围背景与大厅浅色背景统一。
- 设置页只保留浅色/深色模式分段选择。
- 设置标题复用底部设置 Tab 的 `sys.symbol.more` 系统图标。
- 移除设置页中的 iOS / Android 文案，以及未确认的声音、触感反馈设置项。

## 构建命令

```bash
DEVECO_STUDIO_APP=/Volumes/MacSSD/Applications/DevEco-Studio.app \
  ./scripts/build-app.sh --stacktrace
```

## 用例结果

| 用例 | 结果 | 备注 |
| --- | --- | --- |
| ArkTS 编译 | PASS | `:entry:default@CompileArkTS` 通过。 |
| HAP/App 打包 | PASS | `assembleApp` 通过，构建耗时约 7.8 秒。 |
| 签名配置 | WARN | 未配置 `signingConfig`，生成未签名产物；不影响本次代码编译。 |
| 设置页浅色模式 | PENDING | 需要在真机或预览器确认分段控件、标题图标和文字对比度。 |
| 设置页深色模式 | PENDING | 需要确认主题切换、选中态和首页同步。 |
| 折叠屏手机画布 | PENDING | 需要在目标折叠屏/平板确认主页、鸡窝水果摊和其余已接入页面的布局。 |

## 日志与结论

- 构建输出：`BUILD SUCCESSFUL in 7 s 774 ms`。
- 脚本默认使用 `/Applications/DevEco-Studio.app`；本机完整工具链实际位于 `/Volumes/MacSSD/Applications/DevEco-Studio.app`，因此本次通过 `DEVECO_STUDIO_APP` 显式指定。
- 本报告只记录构建检查，未将未执行的真机/预览器验证标记为通过。
