# 成就提醒通用组件 测试报告

- 日期：2026-08-26
- 测试人：Codex
- 基线提交：`607041b feat: connect achievement wall to live state`
- 设备：未连接真机或模拟器
- HarmonyOS 版本：未连接设备，无法读取
- DevEco Studio：本机 `/Volumes/MacSSD/Applications/DevEco-Studio.app`

## 变更范围

- 新增 `shell/AchievementNotice.ets`：将成就引擎的 `newlyUnlocked` 转为通知展示数据；一批最多展示 3 条单项，剩余合并为一条汇总项。
- 新增 `components/AchievementUnlockNotice.ets`：顶部居中、非阻断的 ArkUI “鸡窝勋章”组件。当前复用已有游戏 Logo，并以注册表 `placeholderSymbol` 作为勋章图片未完成前的占位。
- `Index.ets` 作为唯一队列所有者：等待 `GameEntryBadge` 卸载后播放，普通 / 高光时长不同，切换游戏、返回大厅和页面销毁时清理队列与计时器。
- 新增 Hypium `AchievementNotice.test.ets` 并加入 `List.test.ets`；同步扩展 Node 纯逻辑回归。
- 更新成就架构、提醒交互与索引文档，明确当前没有游戏事实上报。
- 未修改 `entry/src/main/ets/games/**`，未改变任何游戏玩法、状态机、页面结构或现有分数上报。

## 构建与测试命令

```bash
node scripts/test-achievement-engine.mjs
DEVECO_STUDIO_APP='/Volumes/MacSSD/Applications/DevEco-Studio.app' ./scripts/build-app.sh --stacktrace
DEVECO_SDK_HOME='/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/sdk' \
  JAVA_HOME='/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/jbr/Contents/Home' \
  /Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw onDeviceTest \
  -p module=entry -p product=default -p coverage=false \
  --node-home /Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/tools/node \
  --no-daemon --stacktrace
/Volumes/MacSSD/Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/toolchains/hdc list targets -v
git diff --check
git diff --name-only -- entry/src/main/ets/games
git status --short -- entry/src/main/ets/games
```

## 用例结果

| 用例 | 结果 | 备注 |
| --- | --- | --- |
| 单条通知展示数据 | PASS | 可从注册表得到游戏归属、标题、短说明、占位符号和展示等级。 |
| 多项解锁折叠 | PASS | 前 3 条逐条展示，第 4 条后合并为汇总项；任一高光成就会使汇总项保持高光等级。 |
| 异常与重复展示数据 | PASS | 重复、禁用、未注册的成就 ID 不生成通知项；空解锁列表返回空队列。 |
| Node 纯逻辑回归 | PASS | `node scripts/test-achievement-engine.mjs` 输出 `Achievement core and notice tests passed`。 |
| ArkTS 主模块构建 | PASS | `./scripts/build-app.sh --stacktrace` 输出 `BUILD SUCCESSFUL`。仅有 unsigned HAP 签名警告。 |
| Hypium 测试源码编译与打包 | PASS | `onDeviceTest` 已完成 `:entry:ohosTest@OhosTestCompileArkTS` 与 `:entry:ohosTest@PackageHap`，新通知 Hypium 用例已进入测试包。 |
| Hypium 真机执行 | BLOCKED | 测试包生成后设备侧任务未能连通；本机 `hdc list targets -v` 无可用目标输出，无法将“测试包编译成功”误报为设备侧执行成功。 |
| 游戏目录范围检查 | PASS | `git diff --name-only -- entry/src/main/ets/games` 与 `git status --short -- entry/src/main/ets/games` 均无输出。 |
| 差异格式检查 | PASS | `git diff --check` 无输出。 |

## 真机待验收项

- 第一款游戏接入后，确认通知在该游戏自己的高光 / 结算反馈完成后出现。
- 确认 2048 滑动、扫雷点击、FreeCell 拖拽、俄罗斯方块按键和 Suika 物理操作不会被浮层吞掉。
- 确认深浅色、小屏和入场身份胶囊的避让效果。
- 恢复可用 HDC / 测试设备后，重新运行 `onDeviceTest`，完成 Hypium 设备侧执行。
