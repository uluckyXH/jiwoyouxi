# 2026-06-11 合成大西瓜玩法对齐测试报告

## 测试范围

- 固定开局生成序列。
- 小等级随机生成池。
- 同级合成、最终等级合成、最大等级不继续合成。
- 参考项目风格计分。
- 失败线持续超线判定。
- 轻量圆形碰撞合成。
- 鸿蒙 ArkTS 构建。

## 自动化测试

```bash
node --no-warnings scripts/test-suika-rules.mjs
```

结果：通过。

覆盖点：

- 开局序列为 `0, 0, 1, 2, 2, 3`。
- 固定序列后随机池限制在 `0..4`。
- 普通合成按 `源等级 + 1` 计分。
- `9 + 9` 合成等级 10，并额外加 100 分。
- 等级 10 不继续升级、不继续加分。
- 新投放物体在释放保护时间内不触发失败线。
- 持续超线达到 3000ms 后触发失败。
- 同级重叠圆形合成为下一等级。

## 回归测试

```bash
node --no-warnings scripts/test-egg-sling-physics.mjs
```

结果：通过。

## 格式检查

```bash
git diff --check
```

结果：通过。

## 构建检查

```bash
./scripts/build-app.sh --stacktrace
```

结果：通过。

备注：构建输出中仍有 `No signingConfig found for product default` 警告，属于当前本地默认签名配置状态，不影响本次 ArkTS 编译和打包通过。

## 待真机验证

- 拖动当前水果时是否足够跟手。
- 松手后 500ms 再出现下一颗的节奏是否自然。
- 失败线闪烁是否明显但不干扰。
- 低端设备上大量水果堆叠时是否卡顿。
- 小屏上顶部 HUD、投放区、容器是否互相遮挡。
