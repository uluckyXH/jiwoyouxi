# 首页 SVG 海报与按钮验证

日期：2026-09-17

测试人：Codex

范围：当前工作区首页海报、开始按钮及其 5 个 SVG 资源

## 改动

- 新增暖阳 / 月夜海报、浅色 / 深色按钮与播放符号，原 `banner.png` 保留。
- 抽出 `HubHeroBanner`，以原生文字叠放 SVG 场景、原生 Button 承载 SVG 按钮底图和播放符号。
- 海报按比例显示，开始按钮独立点击，目标游戏仍使用现有 `heroGameId()`。
- 游戏代码、游戏素材、成就规则、存档和 DEV 功能均未改。

## 环境与命令

DevEco Studio 6.1.1，使用 `/Volumes/MacSSD/Applications/DevEco-Studio.app` 内置 JBR / SDK；项目目标 API 未变更。

```bash
DEVECO_STUDIO_APP=/Volumes/MacSSD/Applications/DevEco-Studio.app \
  ./scripts/build-app.sh --stacktrace
git diff --check
```

日志：`/tmp/jiwoyouxi-home-poster-svg-build.log`。产物为未签名应用，未安装或发布。未执行原生设备测试，无设备型号、实机系统版本或原生截图可记录。

## 结果

| 检查 | 结果 |
| --- | --- |
| ArkTS 编译与打包 | PASS，最终 `BUILD SUCCESSFUL` |
| 新 SVG 结构 | PASS，5 个文件均为静态基本图形，无外链 / 字体 / 位图 |
| HAP 内容 | PASS，5 个新 SVG 与保留 PNG 的内容均和源码一致 |
| 原海报保留 | PASS，与 `HEAD` 中原始 PNG 字节完全一致 |
| 游戏目标选择 | PASS，`heroGameId()` 与基线逐字一致；最近有效游戏优先，默认水果摊 |
| 浅色 / 深色离线预览 | PASS，检查插画、标题、按钮及主题对比 |
| 小屏离线排版 | PASS，以 320vp 屏宽 / 280vp 海报宽度绘制检查，标题和按钮未发生重叠 |
| 游戏改动边界 | PASS，`ets/games/` 与 `rawfile/games/` 的 git diff 为空 |
| 差异格式 | PASS，`git diff --check` |
| 原生点击、字体布局、按压动画和主题切换 | 未执行，仍待设备验收 |

原海报 SHA-256：`55ac32862515a376beb489ee4e6d1ca44f271de29dff56f1255c626d7a07a817`。

本轮为视觉与组件布局修改，未修改规则逻辑，没有新增规则单元测试。

## 视觉记录与后续验收

[海报与按钮预览](../docs/xiaohuang/预览/首页SVG海报与按钮.png)由实际 SVG 资源离线合成，不是原生截图，不能替代 ArkUI 实机布局验证。

设备可用时验证：

1. 浅色 / 深色模式下海报、按钮与文字同步切换；切换后重新进入首页仍正确。
2. 无最近记录时点击“开始游戏”进入水果摊；已有记录时进入最近有效游戏。
3. 点击海报插画不进入游戏；按钮轻点、按压取消均正常，快速连点由现有路由防重逻辑处理。
4. 320vp、390vp 手机宽度下文字完整、按钮未裁切，与下方游戏网格间距正常。
