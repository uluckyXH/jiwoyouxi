# 成就系统完善计划

更新时间：2026-09-17
状态：全部 22 枚 SVG 已绘制并接入；四条通用成就展示已完善，游戏触发缺口维持原状；真机验收待设备

## 1. 已确定的要求

- 首期 22 条成就各自绘制一枚对应语义的 SVG 勋章，由 Codex 直接绘制可编辑的矢量文件。
- 正式素材方案由原来的 PNG 改为 SVG；不再等待外部绘图素材。旧素材文档中的主题可以沿用，PNG 交付和模型生成流程由本记录覆盖。
- 先完善四条跨游戏的通用成就（即“合集里程碑”），绘制其 SVG 并接入现有成就墙的合集分组；其余 18 条游戏专属成就随后按游戏逐款完善。
- 四条通用成就的展示已完成；用户随后要求先补齐其余 18 枚 SVG，本轮已完成全部图标接入，后续再逐款处理游戏触发。

用户已澄清：本次先做的“通用成就”就是以下四条合集里程碑。全部 22 条各自绘制 SVG 是整体素材目标，第一阶段仅处理这四条。

| 通用成就 | 条件 |
| --- | --- |
| 鸡窝开张 | 六款主推游戏中，任意一款首次自然结束一局正式游戏。 |
| 六边形鸡仔 | 六款主推游戏分别自然结束至少一局正式游戏。 |
| 收藏家 | 通过有效收藏操作使主推游戏收藏数达到 3，保存成功后解锁。 |
| 常驻鸡仔 | 六款主推游戏累计自然结束 10 局正式游戏。 |

## 2. 当前代码进度

以下结论来自 2026-09-17 的静态代码核对，不等于真机验收通过。主要依据为 `AchievementRegistry.ets`、`AchievementEngine.ets`、六款游戏页面的 `scheduleAchievementReport()`，以及应用壳和成就墙。

| 模块 | 已有链路 | 尚缺内容 |
| --- | --- | --- |
| 通用模块 | 判定、去重、独立存档、串行保存、成就墙、底部串行提示、DEV 成就重置；四枚通用 SVG 与路径校验已接入 | 原生设备显示与交互验收 |
| 合集里程碑 | 4 / 4：首局、六款游戏、收藏三款、累计十局 | 真机验证收藏、自然结束、重开和退出边界 |
| 扫雷 | 3 / 3：通关、60 秒内通关、正确插旗 | 真机验证事实与提示时机 |
| 石头剪刀布大作战 | 2 / 3：初入战场、押中阵营，结算时上报 | 转化风暴阈值未确定，规则禁用 |
| 空当接龙 | 1 / 3：通关时上报 | 精打细算、疾速清台阈值未确定，规则禁用 |
| 鸡窝 2048 | 3 / 3：自然结束时根据最大方块判定 | 合出 256 / 1024 / 2048 后即时记录与提示 |
| 俄罗斯方块 | 3 / 3：Game Over 时根据四消、行数和等级判定 | 四消、20 行、等级 3 达成时即时记录与提示 |
| 鸡窝水果摊 | 1 / 3：自然结束时根据分数判定 600 分成就 | 600 分即时上报，以及西瓜、最终合成的事实记录和上报 |

22 条目录中，17 条具备当前代码触发链路；3 条规则禁用，另 2 条规则启用但缺少游戏事实上报。不能把注册表里的 `enabled: true` 等同于完整接入，也不能把全部专属成就统称为未开发。

现有测试报告记录应用构建和 Hypium 测试模块编译通过；设备侧 Hypium 执行、实际交互和显示效果仍待验收。

## 3. 尚未接入或完善的具体项目

### 3.1 当前无法通过正常玩法解锁的五条

| 成就 ID | 当前名称 | 原因 |
| --- | --- | --- |
| `rpsBattle.conversion_storm` | 转化风暴 | 转化次数阈值待校准，`enabled: false` |
| `freecell.efficient_win` | 精打细算 | 步数阈值待校准，`enabled: false` |
| `freecell.quick_win` | 疾速清台 | 用时阈值待校准，`enabled: false` |
| `suika.make_watermelon` | 西瓜到手 | 注册规则需要 `madeWatermelon`，游戏未上报 |
| `suika.final_merge` | 瓜王诞生 | 注册规则需要 `isFinalMerge`，游戏未上报 |

### 3.2 已有链路但体验尚未完成

- 2048、俄罗斯方块和水果摊当前只有结算上报；局内达成目标后主动退出，不会因为那次高光立即保留成就。需要追加明确的 `milestone` 事件。
- 全部 22 条成就均已接入独立 SVG；三条禁用规则和两条缺事实上报的成就仅完成配图，解锁状态不变。
- 注册表已支持同一成就目录下的 SVG / PNG，并拒绝目录穿越、子目录和附加参数；对应 Hypium 用例已编译。
- 各游戏结算页尚无“本局新成就”回看区。
- 旧文档中的“历史补发”“顶部提示”“其余游戏全部未接入”等描述与当前代码有差异，需要随各阶段同步校正。

## 4. 第一阶段：四条通用成就（合集里程碑）

### 素材与共享展示

- [x] 绘制并注册四枚 `global.*` SVG，保存到 `entry/src/main/resources/rawfile/app/achievements/`；其余 18 枚已在随后配图阶段补齐。
- [x] 统一暖色、圆润轮廓，分别使用鸡窝与鸡蛋、六片羽毛、爱心收藏夹、日历与羽毛表达成就。
- [x] 使用静态矢量基础图形，未嵌入位图、外部资源、文字、脚本或动画；XML 结构检查通过。
- [x] 列表和详情共用 `AchievementBadge`，未解锁态保留原色并使用中性底板、解锁态显示暖金底和勾选；通知共用注册表的素材路径。
- [x] 路径校验和 Hypium 素材用例已更新。
- [x] 完成浅色、深色及小尺寸的离线 SVG 预览检查，见[图标预览](预览/通用成就SVG预览.png)。
- [ ] 连接设备验证原生 Image 的实际解码与小尺寸显示。离线预览不替代原生验收。

### 页面与状态

- [x] 合集分组接入四枚 SVG，保留独立进度和分组统计，补充自然结算、重开退出不计入等条件文案。
- [x] 详情改为可滚动的独立视图，完整展示条件、状态、来源、日期和进度，避免旧的固定高度裁切长内容。
- [x] 六边形鸡仔详情增加六款主推游戏完成清单，直接读取成就状态中的 `playedGameIds`。
- [x] 收藏家标明进度为已达到的最高值、取消收藏不回退；只允许本次新增且成功保存的主推游戏收藏生成事件。
- [x] 保留现有筛选含义：“进行中”只包含已有正进度且未解锁的项目；已增加四条合集全部解锁后筛选和进度的 Hypium 回归用例。
- [x] 保留常驻 DEV 重置入口，仍只影响独立成就状态。
- [x] 应用与 Hypium 测试模块均已编译通过，四枚 SVG 已包含在 HAP 中。
- [ ] 执行设备侧 Hypium，验收小屏、深浅色、详情滚动、筛选、重启持久化、通知和 DEV 重置。

第一阶段不调整成就 ID、门槛、存档或游戏玩法；不因素材就绪自动启用三条待校准规则。保留现有已解锁记录，继续遵守不补发历史成就的边界。

本阶段对公共展示组件的改动只服务于这四条通用成就的接入与验证；其余游戏专属素材和触发逻辑留到对应游戏阶段。

## 5. 后续顺序

1. 四条通用成就的 SVG、合集分组与详情代码已完成；连接设备后完成原生展示和触发验收。
2. 其余 18 枚 SVG 已补齐；后续补水果摊缺失的合成事实，逐款完成 2048、俄罗斯方块、水果摊的局内高光上报。
3. 根据玩法样本确定乱斗转化次数、接龙步数和用时门槛，再启用对应三条规则。
4. 逐款补结算页“本局新成就”回看区。
5. 完成真机端到端验收，并同步各游戏设计文档和测试报告。

每个阶段完成后在本文件更新状态；只完成编译或静态核对时，不写成真机验收已通过。

## 6. 本轮实现记录

2026-09-17：四枚 SVG、共享徽章组件、可滚动详情、六游戏完成清单、收藏边界修复与 Hypium 回归用例已完成。原成就墙超过 800 行，本轮将徽章和详情提取后降至约 680 行。验证结果见[测试报告](../../../测试报告/2026-09-17-四条通用成就与SVG.md)。

## 7. 第二轮：补齐游戏专属配图

2026-09-17：按用户要求完成六款游戏共 18 枚 SVG，仅更新对应 `badgeAssetPath` 和既有通知测试的素材断言。全部 22 条成就均有独立图标，规则、阈值、启用状态和游戏事实出口未改动。素材预览与验证见[游戏专属 SVG 报告](../../../测试报告/2026-09-17-游戏专属成就SVG配图.md)。

## 8. 标题标识与未解锁态

2026-09-17：用户要求 DEV 按钮暂不隐藏，已原样保留；标题旁新增鸡蛋奖章 SVG。未解锁图标取消灰度和透明度，列表与详情同步采用原色、轻底板和细边框，解锁仍使用暖金底和勾选。构建及资源检查见[验证报告](../../../测试报告/2026-09-17-成就墙Logo与未解锁样式.md)。

## App 非游戏 UI 图标统一

2026-09-17：按用户要求进一步统一整个 App 的非游戏 UI 图标。新增 23 个自绘 SVG，成就分组 Logo、筛选空状态、完成勾选、缺省通知也已接入；DEV 按钮继续保留。游戏内部图标与素材未改。详见 [App 图标记录](../App非游戏UI图标SVG替换.md)。
