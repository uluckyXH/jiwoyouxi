# 新会话交接文档 - 2026-07-07

## 当前仓库状态

- 本地仓库：`/Volumes/MacSSD/Repositories/jiwoyouxi`
- 当前分支：`main`
- 当前提交：`607f418 chore: save pending game docs and dev tools`
- 当前本地 `main` 已推送到个人 fork：
  - `origin -> git@github.com:uluckyXH/jiwoyouxi.git`
  - `origin/main` 当前也是 `607f418`
- 原仓库 remote：
  - `upstream -> git@github.com:longxinzhang/jiwoyouxi.git`
  - `upstream/main` 当前是 `41b0571 fix(app): replace Egg Sling with 2048`
- 当前 `main` 已经包含所有本地游戏分支的主要代码。
- 本地分支没有删除，当前都已经合入 `main`：
  - `feat/chicken-2048`
  - `feat/tetris-polish`
  - `feature/xiaohuang-minesweeper`
- 创建本文档前工作区是干净的；创建本文档后，会多出本文档这个未提交新增文件。

## PR 状态

已向原仓库创建 PR：

- PR 地址：`https://github.com/longxinzhang/jiwoyouxi/pull/5`
- PR 方向：`uluckyXH:main -> longxinzhang:main`
- PR 标题：`feat: polish tetris gameplay and visuals`
- PR 包含提交：
  - `b37760e feat: polish tetris gameplay and visuals`
  - `f76fbdb feat: refine tetris themed experience`
  - `607f418 chore: save pending game docs and dev tools`

PR 创建前已验证：

```bash
git diff --check upstream/main..main
node --no-warnings scripts/test-tetris-rules.mjs
./scripts/build-app.sh --stacktrace
```

结果：

- `git diff --check` 通过。
- 俄罗斯方块规则测试：`28 passed, 0 failed`。
- HarmonyOS 构建：`BUILD SUCCESSFUL`。
- 构建时有 `No signingConfig found for product default` 警告，这是当前本地构建配置的签名警告，不影响本次构建通过。

## 本轮主要工作

### 1. 俄罗斯方块重构和打磨

俄罗斯方块已经从单文件实现拆成多文件，避免后续继续堆在 `TetrisPage.ets` 里。

核心文件：

- `entry/src/main/ets/games/tetris/TetrisPage.ets`
- `entry/src/main/ets/games/tetris/TetrisTypes.ets`
- `entry/src/main/ets/games/tetris/TetrisRules.ets`
- `entry/src/main/ets/games/tetris/TetrisBoard.ets`
- `entry/src/main/ets/games/tetris/TetrisHud.ets`
- `entry/src/main/ets/games/tetris/TetrisPreview.ets`
- `entry/src/main/ets/games/tetris/TetrisControl.ets`
- `entry/src/main/ets/games/tetris/TetrisOverlay.ets`
- `entry/src/main/ets/games/tetris/TetrisTheme.ets`

资源：

- `entry/src/main/resources/rawfile/games/tetris/backgrounds/bg.png`

规则测试：

- `scripts/test-tetris-rules.mjs`

### 2. 玩法和性能

已完成：

- 7-bag 随机队列。
- 3 个 next 预览。
- Hold 暂存，每个方块落地前只能换一次。
- 幽灵落点，也就是浅色影子提示最终落点。
- 软降、硬降、左右长按连续移动、旋转墙踢。
- 锁定延迟和落地后少量调整次数。
- 消行、计分、等级和下落速度。
- 暂停、返回大厅确认、结算弹窗。
- 最高分回写。
- 棋盘绘制改为 Canvas，替代旧的 `Grid + 200 GridItem`，这是解决卡顿的核心改动之一。

当前仍需真机试玩验证：

- 长按左右移动的手感。
- 长按 `↓` 触发硬降是否符合预期。
- Hold 卡片点按命中区域。
- Canvas 棋盘在不同尺寸设备上的清晰度和居中情况。
- 暂停/继续、返回大厅确认、结算、最高分回写全链路。

### 3. 视觉和主题

用户方向：

- 不做复古像素风。
- 目标是鸿蒙原生质感，结合温暖鸡窝主题。
- 主视觉参考图已经删除，不再提交参考图本体。

当前视觉状态：

- 顶部栏已改成小按钮和居中标题/分数，避免遮挡背景人物。
- 棋盘使用暖棕色木框和深色棋盘底。
- 方块使用更圆润、更有高光的质感。
- 左右状态卡、next 和 hold 预览已重新居中。
- 底部操作按钮使用奶油色、黄色、粉色搭配。
- 暂停、退出确认、游戏结束弹窗已统一成同一主题样式。
- 消行有扫光/闪烁反馈，但用户反馈最初不明显，后面已加强过。

如果继续视觉优化，优先看：

- `TetrisTheme.ets`：颜色、尺寸、阴影等视觉 token。
- `TetrisBoard.ets`：棋盘、方块、消行动画绘制。
- `TetrisControl.ets`：底部按钮尺寸、颜色、按压反馈。
- `TetrisOverlay.ets`：暂停、退出、结算弹窗。
- `TetrisHud.ets`：左右信息卡和提示文案。

## 2048 当前补充改动

本轮最后一次提交 `607f418` 也包含 2048 的一个开发辅助入口。

文件：

- `entry/src/main/ets/games/chicken2048/Chicken2048Page.ets`

改动：

- 隐藏 dev 面板改为两行按钮。
- 新增 `超大数字` 按钮。
- 新增 `triggerDevLargeTiles()`。
- 新增 `devPanelHeight()`。

使用方式：

- 连续点击 2048 页面标题 5 次，打开隐藏 dev 面板。
- `超大数字` 会铺一盘从 `4096` 到 `1048576` 的测试数字，用来检查高位数字显示是否溢出。

注意：

- 这是开发阶段调试入口，默认隐藏。
- 如果后续要做正式发版清理，可以考虑移除或保留为内部调试功能。

## 文档和忽略规则

新增/更新的文档包括：

- `docs/开发状态.md`
- `docs/xiaohuang/俄罗斯方块/开源参考.md`
- `docs/xiaohuang/俄罗斯方块/性能优化分析.md`
- `docs/xiaohuang/俄罗斯方块/玩法复刻与性能优化设计.md`
- `docs/xiaohuang/新会话交接文档-2026-06-16.md`
- `测试报告/2026-06-16-俄罗斯方块核心优化.md`

`.gitignore` 已增加软著相关忽略规则：

- `docs/software-copyright-materials/`
- `docs/PDF转换避坑指南.md`
- `docs/软著*`

## 分支和远端关系

远端：

```bash
origin   git@github.com:uluckyXH/jiwoyouxi.git
upstream git@github.com:longxinzhang/jiwoyouxi.git
```

当前关键引用：

```text
main / origin/main: 607f418 chore: save pending game docs and dev tools
upstream/main:      41b0571 fix(app): replace Egg Sling with 2048
feat/tetris-polish: f76fbdb feat: refine tetris themed experience
feat/chicken-2048:  f76fbdb feat: refine tetris themed experience
```

说明：

- `main` 已领先原仓库 `upstream/main`。
- `main` 已推送到个人 fork 的 `origin/main`。
- PR #5 已经把 `origin/main` 提给原仓库。
- 本地旧分支没有删除，后续如果不需要，可以等 PR 合并后再讨论是否清理。

## 常用命令

查看状态：

```bash
git status --short --branch
git log --oneline --decorate --max-count=8
git branch --all --verbose --no-abbrev
```

拉取远端：

```bash
git fetch --all
```

检查原仓库是否有更新：

```bash
git log --oneline main..upstream/main
git rev-list --left-right --count main...upstream/main
```

如果原仓库有新提交，先合并原仓库：

```bash
git switch main
git merge --ff-only upstream/main
```

如果当前 `main` 已经有自己的提交，不能 fast-forward，就不要硬重置，先检查差异再决定合并策略。

推送个人 fork：

```bash
git push origin main
```

测试：

```bash
git diff --check
node --no-warnings scripts/test-tetris-rules.mjs
./scripts/build-app.sh --stacktrace
```

## 新会话建议开场

新会话可以先执行：

```bash
git status --short --branch
git log --oneline --decorate --max-count=8
git fetch --all
gh pr view 5 --repo longxinzhang/jiwoyouxi
```

然后读取本文档和以下文件：

```text
docs/开发状态.md
docs/xiaohuang/俄罗斯方块/性能优化分析.md
docs/xiaohuang/俄罗斯方块/玩法复刻与性能优化设计.md
```

如果继续俄罗斯方块，建议优先做真机试玩反馈，不要先大改结构。当前结构已经拆开，后续小修应该按模块落：

- 规则问题：先改 `TetrisRules.ets`，并补 `scripts/test-tetris-rules.mjs`。
- 触摸手感：优先改 `TetrisControl.ets` 和 `TetrisPage.ets` 的输入调度。
- 棋盘显示：优先改 `TetrisBoard.ets`。
- HUD 和提示：优先改 `TetrisHud.ets`。
- 弹窗：优先改 `TetrisOverlay.ets`。

## 当前风险和注意事项

- PR #5 还没有确认是否已经被原仓库合并。新会话开始后先查 PR 状态。
- 俄罗斯方块虽然规则测试和构建通过，但仍缺最终真机试玩验收。
- 2048 的 `超大数字` 是隐藏开发入口，正式发版前要确认是否保留。
- 不要删除旧分支，用户明确说过暂时不要删分支。
- 不要误删 `FreeCell`。它来自原仓库后续提交，当前主线已经包含。
- 背景图和视觉资源可能后续还会被用户替换，适配时优先保证人物不被顶部栏和底部按键遮挡。

