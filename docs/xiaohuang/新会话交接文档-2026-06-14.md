# 新会话交接文档 - 2026-06-14

## 当前仓库状态

- 本地仓库：`/Volumes/MacSSD/Repositories/jiwoyouxi`
- 当前分支：`main`
- 当前提交：`43f72a9 feat(freecell): restore FreeCell in latest Suika build`
- 本地工作区在创建本文档前是干净的。
- `origin` 指向个人 fork：`git@github.com:uluckyXH/jiwoyouxi.git`
- 原仓库：`git@github.com:longxinzhang/jiwoyouxi.git`
- 当前本地 `main`、`origin/main`、`upstream/main` 已同步到 `43f72a9`。

说明：当前仓库没有配置名为 `upstream` 的 remote，`upstream/main` 是通过下面命令拉取到本地的引用：

```bash
git fetch git@github.com:longxinzhang/jiwoyouxi.git main:refs/remotes/upstream/main
```

## 最近同步记录

- PR #3 已被原仓库合并：
  - PR 地址：`https://github.com/longxinzhang/jiwoyouxi/pull/3`
  - 合并提交：`a7e7a3f Merge PR #3: 优化合成大西瓜后期性能与结算弹窗`
- 原仓库后来新增 FreeCell 提交：
  - `43f72a9 feat(freecell): restore FreeCell in latest Suika build`
  - 已 fast-forward 合并到本地 `main`
  - 已 push 回个人 fork `origin/main`

## 当前重点模块

### 合成大西瓜 `suika`

核心代码：

- `entry/src/main/ets/games/suika/SuikaPage.ets`
- `entry/src/main/ets/games/suika/SuikaPhysics.ets`
- `entry/src/main/ets/games/suika/SuikaTypes.ets`
- `entry/src/main/ets/games/suika/SuikaRules.ets`
- `entry/src/main/ets/games/suika/SuikaResultOverlay.ets`
- `entry/src/main/ets/games/suika/SuikaExitConfirmOverlay.ets`
- `entry/src/main/ets/games/suika/SuikaFeatherLayer.ets`
- `entry/src/main/ets/games/suika/SuikaImageButton.ets`

最近完成：

- 主题改成「鸡窝水果摊」。
- 顶部栏颜色调整：
  - 顶部背景：`#fff0d5`
  - 大厅按钮背景：`#f9dbb2`
  - 顶部文字：`#7b5137`
- 左上吊牌使用 `games/suika/ui/hanging_sign.png`。
- 吊牌内显示：
  - `今日瓜分`
  - `最高纪录`
- 隐藏原来的 `手上 XXX · 下个 XXX` 提示条。
- 投放前水果预览和实际落下水果分离：
  - 预览图：`FRUITS[level].previewImage`
  - 实际游戏图：`FRUITS[level].image`
- 结算弹窗复用扫雷弹窗卡片素材和按钮素材。
- 结算判定：
  - `score < 600`：失败弹窗
  - `score >= 600`：成功弹窗
- 游戏开始后点 `大厅` 会弹确认框；未开始则直接回大厅。
- 修复结算弹窗被水果遮挡的问题：
  - 弹窗显式 `.zIndex(30)`

性能相关已做：

- `SuikaPhysics` 碰撞检测加入平方距离过滤。
- `SuikaPhysics` 加入空间分区候选对，避免每帧全量两两检测。
- `integrateFruits` 改成原地更新，减少每帧临时对象创建。
- `SuikaPage` 背景图从 Canvas 每帧绘制移到 ArkUI 静态图片层。
- 水果数量超过 `SUIKA_STACK_SHADOW_MAX_FRUITS = 18` 后关闭堆叠阴影，只保留底部阴影。
- 增加性能日志：

```text
SuikaPerf fruits=41 frames=60 pairs/frame=... narrow/frame=... hits/frame=... physics=...ms draw=...ms
```

日志开关在 `SuikaPage.ets`：

```ts
const SUIKA_ENABLE_PERF_LOG: boolean = true;
```

后续上线前建议改成 `false`。

需要继续观察：

- 后期水果多时 `pairs/frame` 是否明显低于全量两两检查。
- 目标：`fruits=41` 时不应再是 `820`。
- 如果 `physics` 仍然高，下一步做静止水果降频或稳定堆冻结策略。
- 如果结束弹窗出来时卡，重点不是物理，而是弹窗大图和羽毛图首次解码，需要做预加载或延迟羽毛。

### 扫雷 `minesweeper`

核心代码：

- `entry/src/main/ets/games/minesweeper/MinesweeperPage.ets`
- `entry/src/main/ets/games/minesweeper/MinesweeperResultOverlay.ets`
- `entry/src/main/ets/games/minesweeper/MinesweeperExitConfirmOverlay.ets`
- `entry/src/main/ets/games/minesweeper/MinesweeperImageButton.ets`

当前状态：

- 基础玩法已经可玩。
- 默认 8x8。
- 一种雷，统一叫「鸡雷」，贴图使用 `tile_mine.png`。
- 有结算弹窗、失败/成功卡片、羽毛动画。
- 有返回大厅确认弹窗。
- 分数规则已做过设计，难度选择暂时隐藏。
- 相关素材主要在 `entry/src/main/resources/rawfile/games/minesweeper/`。

注意：

- 扫雷此前做过较多布局和边框调整，素材边框当前以 v1/v3 实际代码为准。
- 如果继续扫雷，要先真机复测顶部按钮、边框拼接、插旗模式提示和结算遮罩。

### 弹射鸡蛋 `eggSling`

核心代码：

- `entry/src/main/ets/games/eggSling/EggSlingPage.ets`

当前状态：

- 做过横屏进入讨论和部分实现。
- 有基础拖拽/发射/地面线/障碍物方向。
- 物理碰撞和倒塌效果仍不成熟。
- 暂时不是当前主线，后续若继续，需要优先重构物理和关卡结构。

文档位置：

- `docs/xiaohuang/弹射鸡蛋/玩法设计.md`
- `docs/xiaohuang/弹射鸡蛋/横屏进入流程.md`
- `docs/xiaohuang/弹射鸡蛋/玩法交互打磨方案.md`
- `docs/xiaohuang/弹射鸡蛋/物理碰撞设计.md`

### FreeCell

原仓库最新提交恢复了 FreeCell：

- `entry/src/main/ets/games/freecell/`
- `entry/src/main/resources/rawfile/games/freecell/`
- `entry/src/main/resources/rawfile/audio/freecell-click.mp3`
- `entry/src/main/resources/rawfile/audio/freecell-hint.mp3`

这是原仓库来的新代码，本地已同步。下一步如果要改大厅或游戏注册表，注意不要误删 FreeCell 注册和资源。

## 文档索引

合成大西瓜：

- `docs/xiaohuang/合成大西瓜/玩法对齐设计.md`
- `docs/xiaohuang/合成大西瓜/视觉风格设计.md`
- `docs/xiaohuang/合成大西瓜/水果素材切图清单.md`
- `docs/xiaohuang/合成大西瓜/水果素材离线边缘处理方案.md`
- `docs/xiaohuang/合成大西瓜/合成反馈音效与特效设计.md`
- `docs/xiaohuang/合成大西瓜/性能卡顿排查与优化方案.md`

扫雷：

- `docs/xiaohuang/扫雷玩法设计.md`
- `docs/xiaohuang/扫雷实现记录.md`
- `docs/xiaohuang/扫雷素材切图记录.md`
- `docs/xiaohuang/扫雷结算弹窗设计.md`
- `docs/xiaohuang/扫雷计分设计.md`
- `docs/xiaohuang/扫雷重构计划.md`

弹射鸡蛋：

- `docs/xiaohuang/弹射鸡蛋/文档索引.md`

## 常用命令

检查状态：

```bash
git status --short
git log --oneline --decorate -8
```

检查原仓库是否有新提交：

```bash
git ls-remote git@github.com:longxinzhang/jiwoyouxi.git refs/heads/main
git fetch git@github.com:longxinzhang/jiwoyouxi.git main:refs/remotes/upstream/main
git rev-list --left-right --count HEAD...upstream/main
git log --oneline HEAD..upstream/main
```

合并原仓库最新代码：

```bash
git merge --ff-only upstream/main
git push origin main
```

验证：

```bash
node --no-warnings scripts/test-suika-rules.mjs
git diff --check
./scripts/build-app.sh --stacktrace
```

## 新会话建议开场

新会话可以先执行：

```bash
git status --short
git log -1 --oneline --decorate
```

然后读取本文档和以下文档：

```text
docs/xiaohuang/合成大西瓜/性能卡顿排查与优化方案.md
docs/xiaohuang/合成大西瓜/玩法对齐设计.md
docs/xiaohuang/扫雷重构计划.md
docs/xiaohuang/弹射鸡蛋/文档索引.md
```

如果继续合成大西瓜，优先看真机日志里的 `SuikaPerf`，不要凭感觉继续优化。

如果继续扫雷，优先真机复测结算弹窗、退出确认和边框拼接。

如果继续弹射鸡蛋，建议先确认是否要引入更可靠的物理方案，当前手写物理还不够稳。
